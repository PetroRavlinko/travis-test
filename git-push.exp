#!/usr/bin/expect

set timeout -1
set remote [lindex $argv 0]
set branch [lindex $argv 1]

spawn git push $remote $branch
expect username*
send "${GIT_USER_ACCOUNT}"\r
expect eof

expect password*
send "${GIT_PASSWORD}"\r
expect eof