language: java
jdk:
  - oraclejdk8
install: ./configure-build-node.sh
after_success:
  - mvn clean
  - test "${TRAVIS_BRANCH}" == "master" && mvn -B release:prepare
  - test "${TRAVIS_TAG}" != "" && mvn -B release:perform
