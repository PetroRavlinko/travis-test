language: java
jdk:
  - oraclejdk8
install: ./configure-build-node.sh
after_success:
  - mvn clean
  - test "${TRAVIS_BRANCH}" == "master" && ./prepare-release.sh
  - test "${TRAVIS_TAG}" != "" && mvn -B release:perform
